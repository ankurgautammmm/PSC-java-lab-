import java.awt.*;

public class AWTExample {
    public static void main(String[] args) {
        if (GraphicsEnvironment.isHeadless()) {
            System.out.println("GUI not supported in online compiler.");
        } else {
            Frame f = new Frame("AWT");
            f.setSize(300, 200);
            f.setVisible(true);
        }
    }
}
